Update Destination
---
tags:
- Parcel
description: This is a PUT request to update destination.

parameters:
- in: path
  name: orderId
  description: order id for specific order
  required: true
  type: integer

- in: header
  name: Authorization
  description: authorization header
  required: true
  type: string
  schema:
  id: bearer
  
- name: Update destination
  in: body
  name: body
  description: update parcel destination.
  required: true
  schema:
    id: user

responses: 
  201:
    description: destination updated succesfully

security:
  - JWT:
      description: Pass in JWT token
      type: apikey
      schema: bearer
      name: Authorization
      in: header
      template: "Bearer {apikey}"
      bearerFormat: JWT